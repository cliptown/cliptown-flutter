name: cliptown_app
description: "Encrypted cross-platform clipboard history and device sync."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  cliptown_interfaces:
    git:
      url: https://github.com/cliptown/cliptown-interfaces.git
      ref: e4e957b5372dc363fe6a52559c8959f0de781efb
      path: generated/dart
  supabase_flutter: ^2.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
